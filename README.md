# FullStack-Graphql-Project
A simple project with NodeTs, React with Vite and graphQl where you create and read a user


## Run `npm install` and `npm run dev` in frontend folder and in backend folder
